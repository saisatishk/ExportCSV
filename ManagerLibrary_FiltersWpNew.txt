<content id="data-content">

<style>

/* ── Design tokens ── */
.filter-panel {
  --fp-green-100: #DCF3E8;
  --fp-green-200: #CAE1D6;
  --fp-green-300: #A7BDB2;
  --fp-green-400: #7E9C8F;
  --fp-green-500: #64796F;
  --fp-green-600: #455950;
  --fp-green-700: #283B33;
  --fp-green-800: #0D2018;
  --fp-ink:   #333333;
  --fp-muted: #737373;
  --fp-rule:  #DADADA;
  --fp-shadow: 0 1px 2px rgba(40,59,51,.06), 0 2px 8px rgba(40,59,51,.05);
}

/* ── Card shell ── */
.filter-panel {
  background: #ffffff;
  border: 1px solid var(--fp-rule);
  border-radius: 14px;
  box-shadow: var(--fp-shadow);
  overflow: hidden;
  font-family: 'Segoe UI', system-ui, -apple-system, Arial, sans-serif;
  font-size: 14px;
  color: var(--fp-ink);
}

/* ── Card header ── */
.fp-header {
  display: flex;
  align-items: center;
  padding: 13px 16px;
  border-bottom: 1px solid var(--fp-rule);
}
.fp-header h3 {
  margin: 0;
  font-size: 14px;
  font-weight: 600;
  color: var(--fp-green-700);
}

/* ── Accordion row — native <details> / <summary>, no JS ── */
details.filter-group {
  border-bottom: 1px solid var(--fp-rule);
}
details.filter-group:last-of-type {
  border-bottom: 0;
}

/* Hide browser's built-in disclosure triangle */
details.filter-group > summary {
  list-style: none;
  cursor: pointer;
}
details.filter-group > summary::-webkit-details-marker { display: none; }
details.filter-group > summary::marker { display: none; }

/* Row header layout */
.fg-toggle {
  display: flex;
  align-items: center;
  width: 100%;
  padding: 12px 16px;
  font-family: inherit;
  font-size: 13px;
  font-weight: 600;
  color: var(--fp-ink);
  user-select: none;
}
.fg-toggle:hover { background: var(--fp-green-100); }

/* Chevron — points right (collapsed) by default */
.fg-caret {
  margin-left: auto;
  color: var(--fp-muted);
  display: flex;
  align-items: center;
  flex: none;
  transform: rotate(-90deg);
  transition: transform .15s;
}
/* Chevron rotates to point down when open */
details.filter-group[open] .fg-caret {
  transform: rotate(0deg);
}

/* Body — shown/hidden by the browser natively */
.fg-body {
  padding: 4px 16px 14px;
}

/* ── ComboBox styling ── */
.fg-body .ms-ComboBox {
  position: relative;
  display: block;
  width: 100%;
  max-width: 100%;
  min-height: 36px;
  background: #fff;
  border: 1px solid #BDBDBD;
  border-radius: 7px;
  box-sizing: border-box;
  overflow: hidden;
}
.fg-body .ms-ComboBox-input {
  display: block;
  width: 100%;
  border: none;
  outline: none;
  background: transparent;
  padding: 0 44px 0 12px;
  color: var(--fp-muted);
  font-family: 'Segoe UI', Arial, sans-serif;
  font-size: 12px;
  line-height: 36px;
  box-sizing: border-box;
}
.fg-body .ms-ComboBox-CaretDown-button {
  position: absolute;
  right: 0; top: 0;
  width: 40px; height: 34px;
  background: var(--fp-green-200);
  border-left: 1px solid #BDBDBD;
  border-top-right-radius: 7px;
  border-bottom-right-radius: 7px;
  box-sizing: border-box;
  cursor: pointer;
  z-index: 2;
}
.fg-body .ms-ComboBox-CaretDown-button .ms-ComboBox-caretDown {
  color: var(--fp-green-500);
  fill: var(--fp-green-500);
}
.fg-body .ms-ComboBox:focus-within {
  border-color: var(--fp-green-400);
  box-shadow: 0 0 0 3px rgba(126,156,143,.25);
}

/* ── DatePicker styling ── */
.fg-body pnp-filterdaterange .ms-DatePicker {
  position: relative;
  display: block;
  width: 100%;
  min-height: 36px;
  background: #fff;
  border: 1px solid #BDBDBD;
  border-radius: 7px;
  box-sizing: border-box;
  overflow: hidden;
  margin-bottom: 8px;
}
.fg-body pnp-filterdaterange .ms-TextField-fieldGroup {
  background: transparent;
  border: none;
  height: 36px;
  display: flex;
  align-items: center;
}
.fg-body pnp-filterdaterange .ms-TextField-field {
  flex: 1 1 auto;
  height: 36px;
  border: none;
  outline: none;
  background: #fff;
  padding: 0 44px 0 12px;
  color: var(--fp-muted);
  font-family: 'Segoe UI', Arial, sans-serif;
  font-size: 12px;
  line-height: 36px;
  box-sizing: border-box;
}
.fg-body pnp-filterdaterange .ms-DatePicker-pickerButton {
  position: absolute;
  right: 0; top: 0;
  width: 40px; height: 36px;
  background: var(--fp-green-200);
  border-left: 1px solid #BDBDBD;
  border-top-right-radius: 7px;
  border-bottom-right-radius: 7px;
  box-sizing: border-box;
  cursor: pointer;
  z-index: 2;
}
.fg-body pnp-filterdaterange .ms-DatePicker-pickerIcon {
  color: var(--fp-green-500);
  fill: var(--fp-green-500);
}
.fg-body pnp-filterdaterange .ms-DatePicker:hover { border-color: #A6A6A6; }
.fg-body pnp-filterdaterange .ms-DatePicker:focus-within {
  border-color: var(--fp-green-400);
  box-shadow: 0 0 0 1px var(--fp-green-400) inset;
}

/* ── Calendar callout ── */
.ms-Callout.ms-DatePicker-callout {
  border: 1px solid #BDBDBD;
  border-radius: 6px;
  overflow: hidden;
}
.ms-DatePicker-day--today { border: 1px solid var(--fp-green-500); }
.ms-DatePicker-day.ms-DatePicker-day--highlighted,
.ms-DatePicker-day:hover { background-color: var(--fp-green-100); }
.ms-DatePicker-monthAndYear { color: var(--fp-green-500); font-weight: 600; }
.ms-DatePicker-goToday { color: var(--fp-green-500); }

/* ── ComboBox callout ── */
.ms-ComboBox-callout .ms-Checkbox:hover { background-color: var(--fp-green-100); }

/* ── Callout Apply / Clear buttons ── */
.ms-Callout .ms-Button.ms-Button--primary,
.ms-Callout .ms-Button.ms-Button--primary:hover,
.ms-Callout .ms-Button.ms-Button--primary:active,
.ms-Callout .ms-Button.ms-Button--primary:focus {
  background-color: #6e8b7d !important;
  color: #ffffff !important;
  border-radius: 40px !important;
  border: none !important;
}
.ms-Callout .ms-Button.ms-Button--primary.is-disabled,
.ms-Callout .ms-Button.ms-Button--primary[disabled] {
  background-color: #f3f5f4 !important;
  color: #000 !important;
  border-radius: 40px;
  border: none;
  cursor: not-allowed;
}
button.ms-Button.ms-Button--default {
  background-color: #f3f5f4;
  border: none;
  border-radius: 40px;
  color: black;
  cursor: pointer;
}
button.ms-Button.ms-Button--default.is-disabled,
button.ms-Button.ms-Button--default[disabled] {
  cursor: not-allowed;
}

/* ── PnP filterMultiActions Apply button ── */
[class^="filterMultiActions_"] button.ms-Button.ms-Button--primary[class^="applyBtn_"]:not(.is-disabled):not([disabled]) {
  background-color: #6e8b7d !important;
  color: #ffffff !important;
  border-radius: 40px !important;
  border: none !important;
  cursor: pointer !important;
  min-height: 36px;
}
[class^="filterMultiActions_"] button.ms-Button.ms-Button--primary[class^="applyBtn_"]:not(.is-disabled):not([disabled]) .ms-Button-flexContainer {
  background-color: transparent !important;
  color: inherit !important;
}
[class^="filterMultiActions_"] button.ms-Button.ms-Button--primary[class^="applyBtn_"]:not(.is-disabled):not([disabled]) .ms-Button-label {
  color: #ffffff !important;
}
[class^="filterMultiActions_"] button.ms-Button.ms-Button--primary[class^="applyBtn_"].is-disabled,
[class^="filterMultiActions_"] button.ms-Button.ms-Button--primary[class^="applyBtn_"][disabled] {
  background-color: #f3f5f4 !important;
  border: none !important;
  border-radius: 40px !important;
  color: black !important;
  cursor: not-allowed !important;
}

/* ── Clear-filter icon ── */
.ms-Label i[data-icon-name="ClearFilter"] {
  color: var(--fp-green-500) !important;
  cursor: pointer;
}

/* ── Force full-width on PnP elements ── */
pnp-filtercombobox,
pnp-filtercombobox .ms-ComboBox-container,
pnp-filtercombobox .ms-ComboBox { width: 100% !important; max-width: 100% !important; }
pnp-filtercombobox .ms-ComboBox-Input { width: 100% !important; }
pnp-filtercombobox .ms-Fabric { display: block !important; width: 90% !important; max-width: 100% !important; }

/* ── Footer ── */
.fp-footer {
  padding: 12px 16px 14px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.fp-more-btn {
  width: 100%;
  background: var(--fp-green-700);
  color: #fff;
  border: 0;
  border-radius: 9px;
  padding: 10px;
  font-family: inherit;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
}
.fp-more-btn:hover { background: var(--fp-green-800); }

.fp-clear-link {
  display: block;
  background: transparent;
  border: 0;
  color: var(--fp-muted);
  font-family: inherit;
  font-size: 12.5px;
  cursor: pointer;
  text-decoration: underline;
  padding: 2px 0;
  text-align: left;
}
.fp-clear-link:hover { color: #EB5757; }

#fluent-default-layer-host .ms-Callout .ms-ComboBox-callout {
  position: fixed !important;
  right: 0 !important;
  top: 64px !important;
}

</style>


<div class="filter-panel">

  <!-- Header -->
  <div class="fp-header">
    <h3>Filters</h3>
  </div>

  <!-- One accordion row per filter — expand/collapse via native <details>, no JS -->
  {{#each @root.filters as |filter|}}
  <details class="filter-group">

    <summary class="fg-toggle">
      {{filter.displayName}}
      <span class="fg-caret">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="m6 9 6 6 6-6"/>
        </svg>
      </span>
    </summary>

    <div class="fg-body">

      {{#eq filter.selectedTemplate "ComboBoxFilterTemplate"}}
        <pnp-filtercombobox
          class="dropdown"
          data-instance-id="{{@root.instanceId}}"
          data-filter-name="{{filter.filterName}}"
          data-is-multi="{{filter.isMulti}}"
          data-show-count="{{filter.showCount}}"
          data-operator="{{filter.operator}}"
          data-theme-variant="{{JSONstringify @root.theme}}">
          {{#each filter.values}}
            <option value="{{value}}" data-selected="{{selected}}" data-count="{{count}}">
              {{name}}
            </option>
          {{/each}}
        </pnp-filtercombobox>
      {{else}}
        {{#eq filter.selectedTemplate "DateRangeFilterTemplate"}}
          <pnp-filterdaterange
            data-instance-id="{{@root.instanceId}}"
            data-filter="{{JSONstringify filter}}"
            data-theme-variant="{{JSONstringify @root.theme}}">
          </pnp-filterdaterange>
        {{else}}
          <pnp-filtersearchbox
            data-filter="{{JSONstringify filter}}"
            data-instance-id="{{@root.instanceId}}">
          </pnp-filtersearchbox>
        {{/eq}}
      {{/eq}}

    </div>
  </details>
  {{/each}}

  <!-- Footer -->
  <div class="fp-footer">
    <button class="fp-more-btn" type="button">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M4 6h16M7 12h10M10 18h4"/>
      </svg>
      More filters
    </button>
    <button class="fp-clear-link" type="button">Clear all filters</button>
  </div>

</div>

</content>
